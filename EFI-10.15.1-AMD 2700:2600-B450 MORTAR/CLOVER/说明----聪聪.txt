目录下有两个配置文件：
config(跑代码无主题).plist
config（有主题不跑代码）.plist



默认是：跑代码而且无主题的配置文件，方便大家安装和排查故障。


安装成功后：就可以使用不跑代码，且带有双系统主题的配置文件。


替换方法：将config（有主题不跑代码）.plist 改名为 ：config.plist  
                 替换覆盖原来的config文件，重启电脑即可。